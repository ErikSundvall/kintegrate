# Node / web
node_modules/
dist/

# Proprietary libs
src/vendor/
trash/

# Cached documentation (downloaded, not committed)
docs-cache/

# NPM authentication (keep auth tokens secret)
# The .npmrc file itself is tracked, but actual auth should be via environment variable
# Never commit actual auth tokens!

# Python venvs that might be left behind
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Environment files with secrets
.env
.env.local
.env*.local

# VS Code
.vscode/
