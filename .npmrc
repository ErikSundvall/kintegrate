# NPM Registry configuration for Kintegrate
# This file configures access to the Better Platform NPM registry

# Better Platform proprietary packages registry
@better:registry=https://hub.better.care/npm/

# Authentication for Better registry - uses environment variable
# Set NPM_BETTER_AUTH in your environment (see README for instructions)
//hub.better.care/npm/:_auth=${NPM_BETTER_AUTH}
//hub.better.care/:_auth=${NPM_BETTER_AUTH}
