describe('MV_akutmall_undervisningsexempel_2a - autogenerated form tests', () => {
  it('spo (numerator) range rule 1 accepts valid values and rejects invalid values (min >= 0, max <= 100)', () => {
    cy.assertRangeSamples({"label":"spo (numerator) range rule 1 accepts valid values and rejects invalid values (min >= 0, max <= 100)","min":0,"max":100,"minOp":">=","maxOp":"<=","validSamples":[0,100,50],"invalidSamples":[-1,101],"expectedUnit":null});
  });

  it('spo (denominator) range rule 2 accepts valid values and rejects invalid values (min >= 100, max <= 100)', () => {
    cy.assertRangeSamples({"label":"spo (denominator) range rule 2 accepts valid values and rejects invalid values (min >= 100, max <= 100)","min":100,"max":100,"minOp":">=","maxOp":"<=","validSamples":[100],"invalidSamples":[99.5,100.5],"expectedUnit":null});
  });

  it('frekvens (magnitude) range rule 3 accepts valid values and rejects invalid values (min >= 0, max <= 200, unit /min)', () => {
    cy.assertRangeSamples({"label":"frekvens (magnitude) range rule 3 accepts valid values and rejects invalid values (min >= 0, max <= 200, unit /min)","min":0,"max":200,"minOp":">=","maxOp":"<=","validSamples":[{"magnitude":0,"unit":"/min"},{"magnitude":200,"unit":"/min"},{"magnitude":100,"unit":"/min"}],"invalidSamples":[{"magnitude":-1,"unit":"/min"},{"magnitude":201,"unit":"/min"},{"magnitude":0,"unit":"/min__INVALID"}],"expectedUnit":"/min"});
  });

  it('frekvens (magnitude) range rule 4 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit /min)', () => {
    cy.assertRangeSamples({"label":"frekvens (magnitude) range rule 4 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit /min)","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"/min"},{"magnitude":999,"unit":"/min"},{"magnitude":499.5,"unit":"/min"}],"invalidSamples":[{"magnitude":-1,"unit":"/min"},{"magnitude":1000,"unit":"/min"},{"magnitude":0,"unit":"/min__INVALID"}],"expectedUnit":"/min"});
  });

  it('systoliskt (magnitude) range rule 5 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit mm[Hg])', () => {
    cy.assertRangeSamples({"label":"systoliskt (magnitude) range rule 5 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit mm[Hg])","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"mm[Hg]"},{"magnitude":999,"unit":"mm[Hg]"},{"magnitude":499.5,"unit":"mm[Hg]"}],"invalidSamples":[{"magnitude":-1,"unit":"mm[Hg]"},{"magnitude":1000,"unit":"mm[Hg]"},{"magnitude":0,"unit":"mm[Hg]__INVALID"}],"expectedUnit":"mm[Hg]"});
  });

  it('diastoliskt (magnitude) range rule 6 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit mm[Hg])', () => {
    cy.assertRangeSamples({"label":"diastoliskt (magnitude) range rule 6 accepts valid values and rejects invalid values (min >= 0, max < 1000, unit mm[Hg])","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"mm[Hg]"},{"magnitude":999,"unit":"mm[Hg]"},{"magnitude":499.5,"unit":"mm[Hg]"}],"invalidSamples":[{"magnitude":-1,"unit":"mm[Hg]"},{"magnitude":1000,"unit":"mm[Hg]"},{"magnitude":0,"unit":"mm[Hg]__INVALID"}],"expectedUnit":"mm[Hg]"});
  });

  it('antal_tidigare_deltaganden_i_detta_lopp range rule 7 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"antal_tidigare_deltaganden_i_detta_lopp range rule 7 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (year) range rule 8 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (year) range rule 8 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (month) range rule 9 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (month) range rule 9 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (day) range rule 10 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (day) range rule 10 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (week) range rule 11 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (week) range rule 11 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (hour) range rule 12 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (hour) range rule 12 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (minute) range rule 13 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (minute) range rule 13 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('tid_efter_start (second) range rule 14 accepts valid values and rejects invalid values (min >= 0)', () => {
    cy.assertRangeSamples({"label":"tid_efter_start (second) range rule 14 accepts valid values and rejects invalid values (min >= 0)","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
  });

  it('totalpoäng range rule 15 accepts valid values and rejects invalid values (min >= 0, max <= 20)', () => {
    cy.assertRangeSamples({"label":"totalpoäng range rule 15 accepts valid values and rejects invalid values (min >= 0, max <= 20)","min":0,"max":20,"minOp":">=","maxOp":"<=","validSamples":[0,20,10],"invalidSamples":[-1,21],"expectedUnit":null});
  });

  it('akutmall_undervisningsexempel_2a required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('context required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('start_time required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('setting required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('time required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('language required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('encoding required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('subject required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('skada required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('namn_på_kroppsställe required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('problem_diagnos_namn required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('category required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('territory required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });

  it('composer required cardinality enforces min 1', () => {
    const min = 1;

    const hasRequiredCardinality = (value) => {
      if (value === null || value === undefined) {
        return false;
      }
      if (Array.isArray(value)) {
        return value.length >= min;
      }
      if (typeof value === 'string') {
        return value.length >= min;
      }
      return min <= 1;
    };

    expect(hasRequiredCardinality(null)).to.equal(false);
    expect(hasRequiredCardinality(undefined)).to.equal(false);

    if (min > 1) {
      const tooShort = Array.from({ length: min - 1 }, () => 'x');
      const enough = Array.from({ length: min }, () => 'x');
      expect(hasRequiredCardinality(tooShort)).to.equal(false);
      expect(hasRequiredCardinality(enough)).to.equal(true);
      expect(hasRequiredCardinality('x'.repeat(min - 1))).to.equal(false);
      expect(hasRequiredCardinality('x'.repeat(min))).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(false);
    } else {
      expect(hasRequiredCardinality('x')).to.equal(true);
      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality(1)).to.equal(true);
      expect(hasRequiredCardinality('')).to.equal(false);
      expect(hasRequiredCardinality([])).to.equal(false);
    }
  });
});
