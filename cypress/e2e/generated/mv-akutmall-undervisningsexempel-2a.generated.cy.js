describe('MV_akutmall_undervisningsexempel_2a - autogenerated form tests', () => {
  describe('validation', () => {
    before(() => { cy.formViewerReady(); });

    beforeEach(() => { cy.resetForm(); });

    it('validates spo is between 0 and 100', () => {
      cy.assertRangeSamples({"label":"validation spo #1","min":0,"max":100,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-1],"expectedUnit":null});
    });

    it('validates spo is between 100 and 100', () => {
      cy.assertRangeSamples({"label":"validation spo #2","min":100,"max":100,"minOp":">=","maxOp":"<=","validSamples":[100],"invalidSamples":[99.5],"expectedUnit":null});
    });

    it('validates frekvens is between 0 and 200', () => {
      cy.assertRangeSamples({"label":"validation frekvens #3","min":0,"max":200,"minOp":">=","maxOp":"<=","validSamples":[{"magnitude":0,"unit":"/min"}],"invalidSamples":[{"magnitude":-1,"unit":"/min"},{"magnitude":0,"unit":"/min__INVALID"}],"expectedUnit":"/min"});
    });

    it('validates frekvens is between 0 and 1000', () => {
      cy.assertRangeSamples({"label":"validation frekvens #4","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"/min"}],"invalidSamples":[{"magnitude":-1,"unit":"/min"},{"magnitude":0,"unit":"/min__INVALID"}],"expectedUnit":"/min"});
    });

    it('validates systoliskt is between 0 and 1000', () => {
      cy.assertRangeSamples({"label":"validation systoliskt #5","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"mm[Hg]"}],"invalidSamples":[{"magnitude":-1,"unit":"mm[Hg]"},{"magnitude":0,"unit":"mm[Hg]__INVALID"}],"expectedUnit":"mm[Hg]"});
    });

    it('validates diastoliskt is between 0 and 1000', () => {
      cy.assertRangeSamples({"label":"validation diastoliskt #6","min":0,"max":1000,"minOp":">=","maxOp":"<","validSamples":[{"magnitude":0,"unit":"mm[Hg]"}],"invalidSamples":[{"magnitude":-1,"unit":"mm[Hg]"},{"magnitude":0,"unit":"mm[Hg]__INVALID"}],"expectedUnit":"mm[Hg]"});
    });

    it('validates antal-tidigare-deltaganden-i-detta-lopp is >= 0', () => {
      cy.assertRangeSamples({"label":"validation antal_tidigare_deltaganden_i_detta_lopp #7","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #8","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #9","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #10","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #11","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #12","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #13","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates tid-efter-start is >= 0', () => {
      cy.assertRangeSamples({"label":"validation tid_efter_start #14","min":0,"max":null,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-0.1],"expectedUnit":null});
    });

    it('validates totalpoäng is between 0 and 20', () => {
      cy.assertRangeSamples({"label":"validation totalpoäng #15","min":0,"max":20,"minOp":">=","maxOp":"<=","validSamples":[0],"invalidSamples":[-1],"expectedUnit":null});
    });
  });

  describe('required', () => {
    before(() => { cy.formViewerReady(); });

    beforeEach(() => { cy.resetForm(); });

    it('requires akutmall-undervisningsexempel-2a', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires context', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires start-time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires setting', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires skada', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires namn-på-kroppsställe', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires problem-diagnos-namn', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires time', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires encoding', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires subject', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires category', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires language', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires territory', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });

    it('requires composer', () => {
      const min = 1;

      const hasRequiredCardinality = (value) => {
        if (value === null || value === undefined) {
          return false;
        }
        if (Array.isArray(value)) {
          return value.length >= min;
        }
        if (typeof value === 'string') {
          return value.length >= min;
        }
        return min <= 1;
      };

      expect(hasRequiredCardinality(null)).to.equal(false);
      expect(hasRequiredCardinality(undefined)).to.equal(false);

      expect(hasRequiredCardinality([1])).to.equal(true);
      expect(hasRequiredCardinality([])).to.equal(false);
    });
  });
});
